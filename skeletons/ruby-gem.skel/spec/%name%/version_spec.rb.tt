require 'spec_helper'

# A smoke test spec to make sure tests actually work

module <%= camel_name %>
  describe VERSION do
    it 'is equal to itself' do
      expect { VERSION == <%= camel_name %>::VERSION }
    end
  end
end
